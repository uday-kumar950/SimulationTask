<%= form_with(model: blind_date) do |form| %>
  <% if blind_date.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(blind_date.errors.count, "error") %> prohibited this blind_date from being saved:</h2>

      <ul>
        <% blind_date.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :employee_ids, style: "display: block" %>  
    <%= blind_date_employee_ids_tag%>
  </div>

  <div class="form-group">
    <%= form.label :lunch_date, style: "display: block" %>
    <%= form.date_field :lunch_date, class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :restaurant_name, style: "display: block" %>
    <%= form.text_field :restaurant_name, class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :group_leader_id, style: "display: block" %>
    <%= blind_date_group_leader_tag%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
  